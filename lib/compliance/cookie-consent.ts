/** localStorage key — keep in sync with cookie banner and analytics gating. */
export const COOKIE_CONSENT_STORAGE_KEY = 'sarap-cookie-consent';

export type CookieConsentValue = 'essential' | 'all';

export const COOKIE_CONSENT_EVENT = 'sarap-cookie-consent-changed';
